export const metadata = {
  title: 'Sign-up',
  description: '',
}

export default function SignUpLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}