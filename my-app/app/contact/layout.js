export const metadata = {
  title: 'Sign-up',
  description: '',
}

export default function ContactLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}