export const metadata = {
  title: 'Sign-in',
  description: '',
}

export default function SignInLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}